"""
Инвертировать один бит (0↔1), не трогая остальные.

XOR (^) с маской: где в маске 1, бит в числе переворачивается;
где в маске 0, бит остаётся как был (0^0=0, 1^0=1).
"""

# 0x16 = 0001 0110
a = 0x16
# 0x08 = 0000 1000 — переворачиваем только бит на позиции 3.
a = a ^ 0x08
print(a)
